'use strict';

// eslint-disable-next-line no-undef
$(document).ready(function() {
  // eslint-disable-next-line no-undef
  $('.main-slider__list').slick({
    'dots': true,
    'arrows': false,
  });
});
